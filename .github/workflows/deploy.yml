name: Deploy to VPS

# CD : sur push main, on teste, puis on rsync le code vers le VPS et on
# rebuild les conteneurs Docker. La clé SSH de déploiement est dans le secret
# SSH_PRIVATE_KEY. Secrets requis : SSH_HOST, SSH_USER, SSH_PRIVATE_KEY, DEPLOY_PATH.
on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: deploy-prod
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Test, rsync & rebuild
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      # ── Gate : les tests backend doivent passer avant tout déploiement ──
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Backend tests
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest
          pytest -q

      # ── Déploiement ──
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add VPS to known_hosts
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          mkdir -p ~/.ssh
          # ssh-keyscan peut échouer ponctuellement (timeout réseau, port 22
          # momentanément filtré). On réessaie plutôt que de planter tout le
          # déploiement au premier échec.
          for attempt in 1 2 3 4 5; do
            if ssh-keyscan -T 15 -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null \
               && grep -q . ~/.ssh/known_hosts; then
              echo "known_hosts récupéré (tentative $attempt)"
              exit 0
            fi
            echo "ssh-keyscan tentative $attempt échouée, nouvel essai dans 10s…"
            sleep 10
          done
          echo "::error::ssh-keyscan a échoué après 5 tentatives (port 22 injoignable depuis le runner ?)"
          exit 1

      - name: Rsync code to VPS
        env:
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          rsync -az --delete \
            --exclude '.git' --exclude 'node_modules' --exclude 'frontend/dist' \
            --exclude '.venv' --exclude 'tmp_upload' --exclude 'output' \
            --exclude 'logs.json' --exclude '.env' --exclude '.pytest_cache' \
            --exclude '__pycache__' \
            ./ "$SSH_USER@$SSH_HOST:$DEPLOY_PATH/"

      - name: Build & restart containers
        env:
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          ssh "$SSH_USER@$SSH_HOST" bash -s "$DEPLOY_PATH" <<'EOF'
            set -euo pipefail
            DEPLOY_PATH="$1"
            cd "$DEPLOY_PATH"
            docker compose up -d --build
            docker image prune -f
          EOF

      - name: Verify backend healthy
        env:
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
        run: |
          ssh "$SSH_USER@$SSH_HOST" bash <<'EOF'
            set -euo pipefail
            for i in $(seq 1 30); do
              status=$(docker inspect -f '{{.State.Health.Status}}' fingec-backend 2>/dev/null || echo none)
              if [ "$status" = "healthy" ]; then echo "fingec-backend healthy"; exit 0; fi
              sleep 2
            done
            echo "fingec-backend pas healthy après 60s"
            docker logs --tail=50 fingec-backend
            exit 1
          EOF
