# Base URL of the FastAPI backend.
# - Dev (local): laisser vide → le proxy Vite (vite.config.ts) gère /process, /download, /logs.
# - Prod (Vercel): renseigner l'URL publique du backend déployé, sans slash final.
#   Exemple : VITE_API_URL=https://fingec-api.onrender.com
VITE_API_URL=
